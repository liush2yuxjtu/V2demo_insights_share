[project]
name = "insights-share"
version = "0.1.0"
description = "CLI-first LAN wiki demo for shared insights"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
